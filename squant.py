import numpy as np
import input

def squant(det, change):
    pfact = -1
    for ops in change:
    #creation
      if change[[1] == 0:
        det.add(change[0])
    #annihilation
      if change[1] == 1:
        det.remove(change[0])
      return det, pfact