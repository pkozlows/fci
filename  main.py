import numpy as np
from condon import condon
import inp

# generate fci matrix
fci = np.ndarray((m,m), dtype=float)
diff - []
for i in fci:
    diff = 
# fci matrix
fci = np.ndarray((m,m), dtype=float)
for i in fci:
    i = condon()

