import numpy as np
from condon import condon
import inp

# 1e integrals
one = np.load("h1e.npy")
# 2e integrals 
two= np.load("h2e.npy")
# number electrons n
n = 6
# number orbital m
m = len(one)

# generate fci matrix
fci = np.ndarray((m,m), dtype=float)
diff - []
for i in fci:
    diff = 
# fci matrix
fci = np.ndarray((m,m), dtype=float)
for i in fci:
    i = condon()

